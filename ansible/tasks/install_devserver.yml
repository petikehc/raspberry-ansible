- name: Create directory for devserver files
  file:
    path: /opt/solarpi/devserver
    state: directory
    mode: '0755'
    owner: '{{ ansible_ssh_user }}'
    group: '{{ ansible_ssh_user }}'

- git:
    repo: https://github.com/petikehc/solarpi.git
    dest: /opt/solarpi/devserver

- lineinfile:
    dest=/home/{{ ansible_ssh_user }}/.bashrc
    line="alias python='/usr/bin/python3'"

- pip:
    requirements=/opt/solarpi/devserver/requirements.txt
    virtualenv=/opt/solarpi/devserver/venv
    virtualenv_python=python3

- lineinfile:
    dest=/home/{{ ansible_ssh_user }}/.bashrc
    line="source /opt/solarpi/devserver/venv/bin/activate"

- template:
    src=templates/systemd/solarpi-scheduler-dev.service
    dest=/lib/systemd/system/solarpi-scheduler-dev.service
    mode=0644
  become: yes
  become_method: sudo

- name: Issue daemon-reload and start the Solarpi Scheduler DEV service
  systemd:
    name: solarpi-scheduler-dev
    state: restarted
    enabled: yes
    masked: no
    daemon_reload: yes
